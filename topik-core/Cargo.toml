[package]
name = "topik-core"
version = "0.1.2"
edition = "2024"
description = "Core traits for topik: typed pub/sub topics for Rust"
license = "MIT"
repository = "https://github.com/railforge/topik"

[dependencies]
bytes = "1"
thiserror = "2"
uuid = { version = "1", optional = true }

[features]
uuid = ["dep:uuid"]
